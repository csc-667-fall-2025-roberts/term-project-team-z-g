<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Login – Hidden Joker Rummy</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="/css/main.css" />
  </head>
  <body class="auth-page">
    <nav class="navbar navbar-dark bg-dark shadow-sm">
      <div class="container">
        <a class="navbar-brand" href="/">Hidden Joker Rummy</a>
      </div>
    </nav>

    <main class="auth-shell">
      <div class="auth-panel">
        <div class="auth-heading text-center">Log In Screen</div>
        <h1 class="auth-title text-center mb-3">Welcome Back</h1>
        <% if (typeof error !== 'undefined') { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <form method="post" action="/auth/login" class="stack gap-3">
          <div class="outline-field">
            <label for="username" class="outline-label">Email or Username</label>
            <input id="username" type="text" name="username" required />
          </div>
          <div class="outline-field">
            <label for="password" class="outline-label">Password</label>
            <input id="password" type="password" name="password" required />
          </div>
          <button type="submit" class="btn-outline">Log In</button>
        </form>
        <div class="auth-footer">
          <span>Don’t have an account?</span>
          <a href="/auth/signup">Sign Up</a>
        </div>
        <div class="auth-footer mt-2">
          <a href="#">Forgot Password?</a>
        </div>
      </div>
    </main>
  </body>
</html>
